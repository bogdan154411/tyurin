Міністерство освіти і науки України Національний технічний університет (Харківський політехнічний інсітут). Факультет комп'ютерних наук та програмної інженерії Кафедра інформатики та інтелектуальної власності.

Лабораторна робота #2. Вступ для програмування. Основи debug

Виконав студент групи КІТ-121в Тюрін Б.О

Проходимо реєстрацію на сайті github.com і створюємо приватну репозиторію programming_tyurin.

Клонуємо створений репозиторій за допомогою команди git clone.
Клонірум файли з командою cample sample_project в programming_tyurin
├── doc
│   └── lab02
├── Doxyfile
├── Makefile
├── README.md
├── src
│   ├── lib.c
│   ├── lib.c.dump
│   ├── lib.h
│   ├── main.c
│   └── main.c.dump
└── test
    ├── test.c
    └── test.c.dump
Для того щоб додати індекс git, використовуємо команду git add файл і фіксуємо за допомогою git commit “Initial copy of sample_project”.

    Переходимо до розділу src і за допомогою команди Nano lib.c ми додаємо новий вид - HUMAN (Людина)
case HUMAN:
                        result = "Людина";
                        break;
Робимо те саме в lib.h.
enum animal_type {
        PIG, /< Свинья */
        COW, /< Корова */
        DOG, /< Собака */
        HUMAN, /< Человек */
        CAT, /**< Кот */
Робимо компіляцію проекту командою make clean prep compile check.

  Перевіряємо зміни за допомогою lldb.
  Фіксуємо зміни git commit.
  Завантажуємо проект на сервер github командою git push

  Висновок:

  В результаті виконання роботи мета була досягнута, всі завдання вирішені в повному обсязі. Дізнався і навчився використовувати команди для роботи з debug.
    
    
