Министерство образования и науки Украины Национальный технический университет(Харьковский политехнический инситут).Факультет компьютерных наук и программной инженерии Кафедра информатики и интеллектуальной собственности.

Лабораторная работа #2. Вступление для программирования.Основы debug

Выполнил студент группы КИТ-121в Тюрин Б.О

Проходим регестрацию на сайте  github.com и создаем приватный репозитория programming_tyurin.

Клонируем созданный репозиторий при помощи команды git clone.

Клонирум файлы с sample_project командой cd в programming_tyurin
├── doc
│   └── lab02
├── Doxyfile
├── Makefile
├── README.md
├── src
│   ├── lib.c
│   ├── lib.c.dump
│   ├── lib.h
│   ├── main.c
│   └── main.c.dump
└── test
    ├── test.c
    └── test.c.dump
    Для того ,чтобы добавить индекс git, используем команду git add файл и фиксируем с помощью git commit “Initial copy of sample_project”. 
    
    Переходим в раздел src и при помощи команды Nano lib.c мы добавляем новый вид - HUMAN (Человек)
    case HUMAN:
                		result = "Людина";
                		break;
                		
     Делаем тоже самое в lib.h.        
     
    enum animal_type {
        PIG, /**< Свинья */
        COW, /**< Корова */
        DOG, /**< Собака */
        HUMAN, /**< Человек */
        CAT, /**< Кот */

  Делаем компиляцию проекта командой make clean prep compile check.
  
  Проверяем изменения при помощи lldb. 
  Фиксируем изменения git commit. 	
  Загружаем проект на сервер github командой git push 	
  
  Вывод:
  
  В результате  выполнения работы цель была достигнута,все задания решены в полном объеме.Узнал и научился использовать команды для работы с debug.
    
  
  
    
    
